using Guidance.Environment;
using Guidance.Gameplay.BackgroundGrid;
using UnityEngine;

namespace Guidance.Global {
  public class Persist : MonoBehaviour {
    [SerializeField] private WallBackgroundController m_WallBackgroundController;
    [SerializeField] private RotateSkybox m_RotateSkybox;
    // Start is called before the first frame update
    void Start() {

    }

    // Update is called once per frame
    void Update() {

    }
  }

}
